export default function visualizeClassName(name) {
    //todo to be deleted, it is not in use

    let className = "";

    if (name === "class1") {
        className = "1 Клас";
    } else if (name === "class2") {
        className = "2 Клас";
    } else if (name === "class3") {
        className = "3 Клас";
    } else if (name === "class4") {
        className = "4 Клас";
    }

    return className;
}
